# Grammar

Primary grammar file:

- `syntaxes/moonchunk.tmLanguage.json`

Covered token groups include:

- shebang and comments
- strings and interpolation
- regex and numeric literals
- declarations, functions, keywords, types
- operators and punctuation

To validate scope behavior in VS Code, run:

- `Developer: Inspect Editor Tokens and Scopes`
